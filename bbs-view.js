/* SiteQuant Pro — BBS presentation. No engineering maths lives here. */
window.SiteQuant.bbsView = (() => {
  const model=window.SiteQuant.bbsModel, ui=window.SiteQuant.ui, html=ui.escape;
  const format=(v,d=2)=>Number(v||0).toLocaleString('en-IN',{minimumFractionDigits:d,maximumFractionDigits:d});
  const badge=(text,modifier='')=>`<span class="bbs-badge ${modifier?`bbs-badge--${modifier}`:''}">${html(text)}</span>`;
  function field(i,key,label,unit='',opts={}){const text=opts.text||false;return`<label class="sq-field bbs-field"><span>${label}${unit?` <em>${unit}</em>`:''}</span><div class="bbs-input-wrap"><input data-bbs-field="${key}" id="bbs-${key}" value="${html(i[key]??'')}" type="${text?'text':'number'}" ${text?'maxlength="240"':'min="0" step="any" inputmode="decimal"'}></div></label>`;}
  function select(i,key,label,options){return`<label class="sq-field bbs-field"><span>${label}</span><select data-bbs-field="${key}" id="bbs-${key}">${options.map(o=>{const v=typeof o==='string'?o:o.value,t=typeof o==='string'?o:o.label;return`<option value="${html(v)}" ${i[key]===v?'selected':''}>${html(t)}</option>`}).join('')}</select></label>`;}
  function diagram(code,compact=false){const paths={A:'M28 60 H232',B:'M45 25 V78 H225',C:'M45 25 V78 H215 V25',I:'M45 78 V25 H215 V78',D:'M20 78 H75 L125 38 H215 V78',E:'M65 25 H215 V92 H65 V25',F:'M65 25 H215 V92 H65 V25',G:'M130 20 A45 45 0 1 0 130 110 A45 45 0 1 0 130 20',J:'M35 78 H190 Q225 78 225 42',T:'M28 60 H232',W:'M45 78 V28 H215',V:'M35 78 H85 L135 35 H225'};const path=paths[code]||paths.A;const hook=code==='E'||code==='F'?'<path d="M65 25 L45 8 M65 92 L45 109"/>':'';return`<svg class="bbs-diagram ${compact?'bbs-diagram--compact':''}" viewBox="0 0 260 120" role="img" aria-label="${code} bar diagram"><g class="bbs-diagram-guides">${code==='I'?'<path d="M45 91v18M215 91v18M45 103h170"/>':code==='C'?'<path d="M45 91v18M215 91v18M45 103h170"/>':''}</g><path class="bbs-diagram-bar" d="${path}"/>${hook}</svg>`;}
  function shapeCards(i){const choices=model.shapeOptions.filter(s=>s.calculation==='engine'&&s.applicableMembers.includes(i.memberType));return`<div class="bbs-shape-grid">${choices.map(s=>`<button type="button" class="bbs-shape-card ${i.shape===s.code?'is-selected':''}" data-bbs-shape="${s.code}" aria-pressed="${i.shape===s.code}"><span class="bbs-shape-card-code">${s.code}</span>${diagram(s.code,true)}<b>${html(s.name)}</b><small>${s.code==='I'?'Inverted orientation':'Engine available'}</small></button>`).join('')}</div>`;}
  function autoDimensions(i){const c=Number(i.cover)||0,d=Number(i.dia)||0; if(i.shape==='E'||i.shape==='F'){const w=Math.max(1,Number(i.breadth)*1000-2*c-d),dep=Math.max(1,Number(i.depth)*1000-2*c-d);return`<div class="bbs-auto-dim"><div><span>Derived centreline width</span><b>${format(w,0)} mm</b></div><div><span>Derived centreline depth</span><b>${format(dep,0)} mm</b></div><button type="button" class="bbs-inline-edit" data-bbs-action="edit-link-dim">Edit dimensions</button></div>`;}if(['C','I'].includes(i.shape)){const base=Math.max(1,Number(i.length)*1000-2*c),leg=Math.max(1,Number(i.depth)*1000-2*c);return`<div class="bbs-auto-dim"><div><span>Base / clear bar axis</span><b>${format(base,0)} mm</b></div><div><span>Leg 1 / Leg 2</span><b>${format(leg,0)} mm each</b></div><span class="bbs-auto-note">Derived from member geometry and cover. Drawing override remains available.</span></div>`;}return`<p class="bbs-field-hint">Shape dimensions are derived from the member geometry where applicable. Override only when the drawing specifies a different bar axis.</p>`;}
  function stirrupZones(i){return`<section class="bbs-zones"><div class="bbs-section-title"><div><span class="sq-eyebrow">STIRRUP ZONES</span><h3>Spacing by beam zone</h3></div><span class="bbs-mini-pill">Automatic count</span></div><div class="bbs-zone-grid">${[['zone1Length','zone1Spacing','Zone 1'],['zone2Length','zone2Spacing','Zone 2'],['zone3Length','zone3Spacing','Zone 3']].map(([l,s,n])=>`<div class="bbs-zone"><b>${n}</b>${field(i,l,'Length','mm')}${field(i,s,'Spacing','mm c/c')}</div>`).join('')}</div><p class="bbs-field-hint">Counts are derived from each zone length and spacing. The final zone can be set to 0 when the drawing uses fewer zones.</p></section>`;}
  function lapPanel(i){return`<details class="bbs-advanced"><summary>Fabrication & laps</summary><div class="bbs-fields bbs-fields--three">${field(i,'stockLength','Stock length','mm')}${select(i,'lapMode','Lap strategy',[{value:'AUTOMATIC',label:'Automatic'},{value:'DRAWING_SPECIFIED',label:'Drawing specified'}])}${field(i,'lapLength','Lap length','mm')}</div><p class="bbs-field-hint">Automatic mode uses stock length and diameter-based planning only as a fabrication aid. Final lap detailing remains drawing/code controlled.</p></details>`;}
  function parameters(i){let body='';if(['E','F'].includes(i.shape)){body=`${autoDimensions(i)}${select(i,'linkDetailingMode','Hook detailing',[{value:'SEISMIC_IS13920_2016',label:'135° — IS 13920 workflow'},{value:'GENERAL_IS2502_REFERENCE',label:'General — IS 2502 reference'},{value:'DRAWING_SPECIFIED',label:'Drawing specified'}])}<div class="bbs-hook-rule"><b>Hook rule</b><span>${i.linkDetailingMode==='SEISMIC_IS13920_2016'?'135° hooks · extension derived by the engine':'Use the selected drawing/reference convention.'}</span></div>${stirrupZones(i)}`;}else{body=`${autoDimensions(i)}${i.shape==='B'?field(i,'ret','Return length','mm'):''}${i.shape==='D'?`<div class="bbs-fields bbs-fields--three">${field(i,'rise','Crank rise','mm')}${field(i,'run','Crank run','mm')}${field(i,'tail','Tail','mm')}</div>`:''}${['C','I'].includes(i.shape)?`<div class="bbs-fields bbs-fields--three">${field(i,'uBaseLength','Base override','mm')}${field(i,'uLeg1','Leg 1 override','mm')}${field(i,'uLeg2','Leg 2 override','mm')}</div>`:''}`;}return`<div class="bbs-parameters"><div class="bbs-parameter-heading"><div><span class="sq-eyebrow">DETAIL</span><h3>${html(model.shapeOptions.find(s=>s.code===i.shape)?.name||'Shape')}</h3></div>${diagram(i.shape,true)}</div>${body}${lapPanel(i)}</div>`;}
  function form(i){const memberFamilies={Beam:['Bottom main','Top main','Extra top / support','Extra bottom','Side-face','Curtailment','Stirrups','Distribution'],Column:['Main vertical','Column ties','Confinement','Starter / dowel'],Slab:['Main','Distribution','Extra top / support','Curtailment','Chairs','Cranked'],Footing:['Bottom main','Distribution','Top main','Starter / dowel','U-bar'],};const fams=memberFamilies[i.memberType]||model.families;return`<form class="bbs-editor" id="bbs-editor" novalidate><div class="bbs-form-section"><div class="bbs-section-title"><div><span class="sq-eyebrow">01 · MEMBER</span><h2>What are we scheduling?</h2></div><span class="bbs-step-state">Primary input</span></div><div class="bbs-fields bbs-fields--two">${select(i,'memberType','Structural member',model.members)}${select(i,'family','Reinforcement purpose',fams)}${field(i,'mark','Bar mark','',{text:true})}${field(i,'description','Description','',{text:true})}</div></div><div class="bbs-form-section"><div class="bbs-section-title"><div><span class="sq-eyebrow">02 · PROJECT CONTROL</span><h3>Drawing & revision</h3></div></div><div class="bbs-fields bbs-fields--three">${field(i,'drawingNo','Drawing no.','',{text:true})}${select(i,'revision','Revision',['Draft','R1','R2','R3'])}${field(i,'preparedBy','Prepared by','',{text:true})}</div></div><div class="bbs-form-section"><div class="bbs-section-title"><div><span class="sq-eyebrow">03 · MEMBER GEOMETRY</span><h3>Member dimensions</h3></div></div><div class="bbs-fields bbs-fields--three">${field(i,'length','Length','m')}${field(i,'breadth','Breadth','m')}${field(i,'depth','Depth','m')}</div><div class="bbs-fields bbs-fields--two">${field(i,'cover','Clear cover','mm')}${field(i,'quantity','Identical members','nos')}</div></div><div class="bbs-form-section"><div class="bbs-section-title"><div><span class="sq-eyebrow">04 · REINFORCEMENT</span><h3>Size, quantity & arrangement</h3></div></div><div class="bbs-fields bbs-fields--three">${field(i,'dia','Bar diameter','mm')}${field(i,'spacing','Spacing','mm c/c')}${select(i,'direction','Bar arrangement',[{value:'Longitudinal',label:'Main'},{value:'Transverse',label:'Distribution'}])}</div><p class="bbs-field-hint" id="bbs-direction-hint">${i.direction==='Transverse'?'Bars run across the primary member axis.':'Bars run along the primary member axis.'}</p></div><div class="bbs-form-section"><div class="bbs-section-title"><div><span class="sq-eyebrow">05 · BAR DETAIL</span><h3>Shape & detailing</h3></div></div>${shapeCards(i)}<div id="bbs-shape-parameters">${parameters(i)}</div></div><div class="bbs-form-section bbs-form-footer"><div id="bbs-form-feedback" class="bbs-form-feedback"></div><button type="button" class="sq-button sq-button--primary" id="bbs-add-item">Add BBS item</button></div></form>`;}
  function result(assessment,input){if(!assessment.result)return`<div class="bbs-result-empty"><b>Calculation paused</b><p>${html(assessment.errors.calculation||Object.values(assessment.errors)[0]||'Complete the required inputs.')}</p></div>`;const r=assessment.result,o=r.output,t=r.trace||r.traceLines||[];const lap=Number(o.cuttingLengthM||0)*1000>Number(input.stockLength||12000);return`<div class="bbs-result-card"><div class="bbs-result-hero"><div><span class="sq-eyebrow">CALCULATED</span><h2>${format(o.cuttingLengthM*1000,0)} mm <small>cut length</small></h2><p>${o.totalBars} bars · ${format(o.totalLengthM,2)} m · ${format(o.totalWeightKg,2)} kg</p></div>${badge('Calculated','calculated')}</div><details class="bbs-how"><summary><span>How calculated</span><span>›</span></summary><div><div class="bbs-formula-grid">${(t.length?t:[`Cut length = ${format(o.cuttingLengthM*1000,0)} mm`]).map(x=>`<div>${html(String(x))}</div>`).join('')}</div>${lap?`<div class="bbs-lap-alert"><b>Stock length check</b><span>${format(o.cuttingLengthM*1000,0)} mm exceeds ${format(input.stockLength,0)} mm stock. Fabrication segmentation required.</span></div>`:''}<p class="bbs-code-note">Formula version: ${html(r.engineVersion||'versioned engine')} · Dimension basis retained in the calculation input.</p></div></details></div>`;}
  function scheduleRows(rows,selected,editingId){return rows.map(row=>{const r=model.calculate(row.input).result,o=r?.output;return`<tr class="${editingId===row.id?'is-editing':''}"><td><input type="checkbox" data-bbs-select="${row.id}" ${selected.has(row.id)?'checked':''}></td><td><b>${html(row.input.mark)}</b><small>${row.kind==='local'?'Local draft':'Sample'}</small></td><td>${html(row.input.memberType)}</td><td>${html(row.input.family)}</td><td>${html(row.input.description)}</td><td>${html(row.input.shape)}</td><td>Ø${html(row.input.dia)} · ${o?format(o.cuttingLengthM,2):'—'} m</td><td>${o?format(o.totalWeightKg,2):'—'} kg</td><td><button type="button" class="bbs-row-action" data-bbs-row-edit="${row.id}">Edit</button></td></tr>`}).join('');}
  function summary(analysis){return`<div class="bbs-summary-card"><div><span>Total BBS steel</span><b>${format(analysis.total.weight,2)} kg</b><small>${analysis.total.bars} bars · ${format(analysis.total.length,2)} m</small></div><div><span>Wastage ${analysis.wastage.percent}%</span><b>${format(analysis.wastage.procurementKg,2)} kg</b><small>Procurement estimate</small></div></div>`;}
  function render(projects,project,w){const i=w.draft;return`<section class="bbs-workspace"><header class="bbs-workspace-head"><div><span class="sq-eyebrow">ENGINEERING WORKSPACE</span><h1>Bar bending schedule</h1><p>Build the reinforcement schedule from member purpose, then verify the deterministic calculation.</p></div><div class="bbs-head-actions"><label class="bbs-project-select"><span>Project</span><select id="bbs-project">${projects.map(p=>`<option value="${html(p.id)}" ${p.id===project.id?'selected':''}>${html(p.name)}</option>`).join('')}</select></label><button class="sq-button sq-button--secondary" id="bbs-export-current" disabled>Export current</button></div></header><div class="bbs-project-strip"><span>Drawing <b>${html(i.drawingNo||'Not set')}</b></span><span>Revision <b>${html(i.revision)}</b></span><span>Prepared by <b>${html(i.preparedBy||'—')}</b></span><span id="bbs-save-status">Browser-local draft</span></div><div id="bbs-mobile-summary"></div><div class="bbs-layout"><div class="bbs-primary-column">${form(i)}</div><aside class="bbs-result-column"><details class="bbs-calculation-drawer" id="bbs-verification" open><summary><span>Calculation verification</span><span id="bbs-calculation-state">${badge('Calculated','calculated')}</span><span>›</span></summary><div id="bbs-result">${result({result:null,errors:{}},i)}</div></details></aside></div><section class="bbs-register"><div class="bbs-register-head"><div><span class="sq-eyebrow">PROJECT REGISTER</span><h2>BBS schedule <span id="bbs-row-count">${w.rows.length}</span></h2><p id="bbs-live-status">Calculated records are ready for schedule assembly.</p></div><button class="sq-button sq-button--secondary" id="bbs-export-schedule">Export view</button></div><div class="bbs-register-tools"><input id="bbs-search" type="search" placeholder="Search mark, family, member…"><select id="bbs-filter"><option value="all">All items</option><option value="local">Local drafts</option><option value="sample">Sample</option></select><select id="bbs-sort"><option value="mark-asc">Mark A–Z</option><option value="weight-desc">Weight high–low</option></select><select id="bbs-group"><option value="none">No grouping</option><option value="member">Member</option><option value="dia">Diameter</option><option value="shape">Shape</option></select></div><div id="bbs-summaries">${summary(model.analysis(project.id))}</div><div class="bbs-register-actions"><span id="bbs-selection-label">No items selected</span><button id="bbs-duplicate" disabled>Duplicate</button><button id="bbs-delete" disabled>Delete</button><button id="bbs-add-new" class="bbs-text-action">+ New item</button></div><div class="sq-table-scroll"><table class="bbs-schedule"><thead><tr><th><input id="bbs-select-all" type="checkbox"></th><th>MARK</th><th>MEMBER</th><th>FAMILY</th><th>DESCRIPTION</th><th>SHAPE</th><th>CUT LENGTH</th><th>WEIGHT</th><th>ACTION</th></tr></thead><tbody id="bbs-schedule-body">${scheduleRows(w.rows,new Set(),w.editingId)}</tbody></table></div><p id="bbs-visible-count" class="bbs-register-foot">${w.rows.length} items</p></section><p class="bbs-engine-note">Versioned calculation engine. Engineering review remains a release responsibility before construction issue.</p></section>`;}
  return{format,badge,diagram,parameters,form,result,scheduleRows,render};
})();
